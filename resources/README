# How to Build?
1. create symlink of ```resource``` directory in ```src```
	* important, or the game wouldn't be able to load assets!
2. create ```build``` directory
3. run ```cd build && cmake ..```
